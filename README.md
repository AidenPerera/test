# test

This repository demonstrates a simple username and password verification using Python.

Run the script with `python login.py` and enter the credentials when prompted.
The default username is `admin` and the password is `secret`.
